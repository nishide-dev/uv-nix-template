# Copier template configuration for uv-nix-template
# This template creates a modern Python library project with uv package management

_min_copier_version: "9.0.0"

# ============================================================================
# User questions - collected when generating a new project
# ============================================================================

project_name:
  type: str
  help: "プロジェクト名を入力してください (Enterでディレクトリ名を使用)"
  default: "{{ _copier_conf.dst_path.name }}"

package_name:
  type: str
  help: "パッケージ名を入力してください (Pythonのインポート名、通常はスネークケース)"
  default: "{{ project_name | replace('-', '_') }}"

description:
  type: str
  help: "プロジェクトの短い説明を入力してください"
  default: "A modern Python library"

author_name:
  type: str
  help: "作者名を入力してください"
  default: "Your Name"

author_email:
  type: str
  help: "作者のメールアドレスを入力してください"
  default: "your.email@example.com"

python_version:
  type: str
  help: "対象とする最小Pythonバージョンを選択してください"
  default: "3.12"
  choices:
    - "3.10"
    - "3.11"
    - "3.12"
    - "3.13"

use_ruff:
  type: bool
  help: "Ruff (高速linter/formatter) を使用しますか？"
  default: true

use_ty:
  type: bool
  help: "ty (高速型チェッカー by Astral) を使用しますか？"
  default: true

use_pytest:
  type: bool
  help: "Pytest (テストフレームワーク) を使用しますか？"
  default: true

use_github_actions:
  type: bool
  help: "GitHub Actions CI/CDを設定しますか？"
  default: true

include_docker:
  type: bool
  help: "Docker環境（開発用・本番用）を含めますか？"
  default: false

use_nix:
  type: bool
  help: "Nix + direnv による開発環境管理を使用しますか？"
  default: true

setup_direnv_hook:
  type: bool
  help: "シェル設定ファイル（~/.bashrc or ~/.zshrc）にdirenv hookを自動追加しますか？"
  default: false
  when: "{{ use_nix }}"

# ============================================================================
# File/directory exclusion settings
# ============================================================================

_exclude:
  - "copier.yml"
  - "copier.yaml"
  - ".git"
  - ".venv"
  - "__pycache__"
  - "*.pyc"
  - ".pytest_cache"
  - ".mypy_cache"
  - ".ruff_cache"
  - "uv.lock"  # ロックファイルは生成時に新規作成
  - "dist"
  - "build"
  - "*.egg-info"
  - ".github/ISSUE_TEMPLATE"  # テンプレートリポジトリ用のIssueテンプレート（生成プロジェクトには含めない）
  - ".github/pull_request_template.md"  # テンプレートリポジトリ用のPRテンプレート（生成プロジェクトには含めない）
  - "{% if not use_ruff %}ruff.toml.jinja{% endif %}"  # Ruff未使用の場合は除外
  - "{% if not use_nix %}flake.nix.jinja{% endif %}"  # Nix未使用の場合は除外
  - "{% if not use_nix %}.envrc{% endif %}"  # Nix未使用の場合は除外
  - "{% if not use_nix %}docs{% endif %}"  # Nix未使用の場合はdocsディレクトリ全体を除外
  - "{% if not include_docker %}Dockerfile.dev.jinja{% endif %}"  # Docker未使用の場合は除外
  - "{% if not include_docker %}Dockerfile.prod.jinja{% endif %}"  # Docker未使用の場合は除外
  - "{% if not include_docker %}docker-compose.yml.jinja{% endif %}"  # Docker未使用の場合は除外
  - "{% if not include_docker %}.dockerignore.jinja{% endif %}"  # Docker未使用の場合は除外
  - "{% if not include_docker %}docs/DOCKER.md.jinja{% endif %}"  # Docker未使用の場合は除外

# ============================================================================
# Post-generation tasks
# ============================================================================

_tasks:
  - "git init -b main"
  - "git add ."
  - |
    {% if use_nix and setup_direnv_hook %}
    # シェル設定ファイルへのdirenv hook追加
    for rcfile in ~/.bashrc ~/.zshrc; do
      if [ -f "$rcfile" ] && ! grep -q "direnv hook" "$rcfile"; then
        shell=$(basename "$rcfile" | sed 's/^\.//;s/rc$//')
        echo 'eval "$(direnv hook '"$shell"')"' >> "$rcfile"
        echo "✅ Added direnv hook to $rcfile"
      fi
    done
    {% endif %}
  - "uv venv"
  - "uv lock"
  - "git add uv.lock"
  - "uv sync"
  - |
    set -e
    {% if use_ruff %}
    echo "🔍 Ruff check..."
    uv run ruff check . --fix
    echo "✨ Ruff format..."
    uv run ruff format .
    {% endif %}
    {% if use_ty %}
    echo "🔎 Type checking..."
    uv run ty check
    {% endif %}
    {% if use_pytest %}
    echo "🧪 Running tests..."
    uv run pytest
    {% endif %}
    echo "✅ All quality checks passed!"

  # 成功メッセージ
  - |
    echo "
    ============================================================
    🎉 プロジェクトが正常に作成されました！
    ============================================================

    次のステップ:
      1. プロジェクトディレクトリに移動:
         cd {{ project_name }}

      2. 初回コミット (ファイルは既にステージング済み):
         git commit -m \"chore: initial commit\"

      3. 開発を開始 (仮想環境は既にセットアップ済み):
         uv add <package-name>      # 依存関係の追加
         uv run pytest              # テストの実行
         uv run ruff format .       # コードフォーマット
         uv run ty check            # 型チェック

      4. (オプション) PyTorch/CUDA環境が必要な場合:
         copier copy --trust gh:nishide-dev/uv-torch-nix-template .

    ============================================================
    "

# ============================================================================
# Template metadata
# ============================================================================

_metadata:
  template_version: "0.1.0"
  min_uv_version: "0.4.0"
